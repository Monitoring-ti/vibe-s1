import { SessionResults } from "@/components/session-results";

export const metadata = {
  title: "Resultados de Sesión — VIBE Safety AI",
};

export default function ResultadosPage() {
  return <SessionResults />;
}